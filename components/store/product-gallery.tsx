"use client";
import Image from "next/image";
import { useState } from "react";
export function ProductGallery({ images, name }: { images: { url: string; alt: string }[]; name: string }) { const [active, setActive] = useState(0); const image = images[active]; return <div className="space-y-3"><div className="relative aspect-square overflow-hidden rounded-xl bg-zinc-100">{image ? <Image src={image.url} alt={image.alt || name} fill priority sizes="(max-width: 1024px) 100vw, 50vw" className="object-cover" /> : <span className="grid h-full place-items-center text-sm text-zinc-500">Image bientôt disponible</span>}</div>{images.length > 1 && <div className="flex gap-2 overflow-x-auto pb-1">{images.map((item, index) => <button key={item.url} onClick={() => setActive(index)} className={`relative h-16 w-16 shrink-0 overflow-hidden rounded-md border-2 ${active === index ? "border-black" : "border-transparent"}`}><Image src={item.url} alt="" fill sizes="64px" className="object-cover" /></button>)}</div>}</div>; }
