"use client";
import Image from "next/image";
import Link from "next/link";
import { LoaderCircle, Minus, Plus, Trash2 } from "lucide-react";
import { useState, useTransition } from "react";
import { beginWhatsAppOrder } from "@/app/actions/order";
import { useCartStore } from "@/stores/cart-store";
export function CartPage() { const { items, remove, setQuantity, clear } = useCartStore(); const [pending, startTransition] = useTransition(); const [error, setError] = useState(""); const total = items.reduce((sum, item) => sum + item.unitPrice * item.quantity, 0); const submit = (formData: FormData) => { setError(""); startTransition(async () => { try { const url = await beginWhatsAppOrder({ customerName: String(formData.get("customerName") || "").trim() || undefined, customerAddress: String(formData.get("customerAddress") || "").trim() || undefined, customerPhone: String(formData.get("customerPhone") || "").trim() || undefined, items }); clear(); window.location.assign(url); } catch (cause) { setError(cause instanceof Error ? cause.message : "La commande n’a pas pu être préparée."); } }); }; if (!items.length) return <main className="mx-auto max-w-3xl px-4 py-16 text-center sm:px-6"><h1 className="text-3xl font-bold">Votre panier est vide</h1><p className="mt-3 text-zinc-600">Découvrez les articles disponibles dans notre catalogue.</p><Link href="/catalogue" className="mt-6 inline-flex rounded-lg bg-black px-5 py-3 font-medium text-white">Voir le catalogue</Link></main>; return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6"><h1 className="text-3xl font-bold">Votre panier</h1><div className="mt-7 grid gap-8 lg:grid-cols-[minmax(0,1fr)_360px]"><section className="divide-y rounded-xl border">{items.map((item) => <article key={item.key} className="flex gap-3 p-3 sm:gap-4 sm:p-4"><div className="relative h-20 w-20 shrink-0 overflow-hidden rounded-md bg-zinc-100">{item.imageUrl && <Image src={item.imageUrl} alt={item.productName} fill sizes="80px" className="object-cover" />}</div><div className="min-w-0 flex-1"><div className="flex gap-2"><div className="min-w-0 flex-1"><h2 className="truncate font-semibold">{item.productName}</h2>{item.variantName && <p className="text-sm text-zinc-500">{item.variantName}</p>}</div><button aria-label="Retirer du panier" onClick={() => remove(item.key)} className="p-1 text-zinc-500 hover:text-red-700"><Trash2 size={18} /></button></div><div className="mt-3 flex items-center justify-between"><div className="flex items-center rounded-md border"><button onClick={() => setQuantity(item.key, item.quantity - 1)} className="p-2" aria-label="Réduire la quantité"><Minus size={15} /></button><span className="min-w-8 text-center text-sm">{item.quantity}</span><button onClick={() => setQuantity(item.key, item.quantity + 1)} className="p-2" aria-label="Augmenter la quantité"><Plus size={15} /></button></div><p className="font-semibold">₣{(item.unitPrice * item.quantity).toLocaleString("fr-FR")}</p></div></div></article>)}</section><aside className="h-fit rounded-xl border p-5 lg:sticky lg:top-20"><h2 className="text-xl font-bold">Finaliser la commande</h2><p className="mt-2 text-sm text-zinc-600">Tu seras redirigé vers WhatsApp pour confirmer le paiement et la livraison.</p><form action={submit} className="mt-5 space-y-3"><input name="customerName" placeholder="Nom complet" className="w-full rounded-lg border px-3 py-2.5" /><input name="customerPhone" type="tel" placeholder="Téléphone" className="w-full rounded-lg border px-3 py-2.5" /><textarea name="customerAddress" placeholder="Adresse de livraison" rows={3} className="w-full rounded-lg border px-3 py-2.5" />{error && <p className="text-sm text-red-700">{error}</p>}<div className="flex items-center justify-between border-t pt-4"><span className="font-medium">Total</span><strong className="text-xl">₣{total.toLocaleString("fr-FR")}</strong></div><button disabled={pending} className="flex w-full items-center justify-center gap-2 rounded-lg bg-emerald-600 px-4 py-3 font-semibold text-white disabled:opacity-60">{pending && <LoaderCircle className="animate-spin" size={18} />}{pending ? "Préparation…" : "Commander via WhatsApp"}</button></form></aside></div></main>; }
