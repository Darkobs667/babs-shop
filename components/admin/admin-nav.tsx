"use client";
import Link from "next/link";
import { BarChart3, LayoutDashboard, LogOut, Menu, PackageSearch, ShoppingBag, X } from "lucide-react";
import { usePathname } from "next/navigation";
import { useState } from "react";
import { logout } from "@/app/admin/logout/actions";
const links=[{href:"/admin",label:"Tableau de bord",icon:LayoutDashboard},{href:"/admin/products",label:"Produits",icon:PackageSearch},{href:"/admin/analytics",label:"Analytics",icon:BarChart3}];
export function AdminNav(){const pathname=usePathname();const[open,setOpen]=useState(false);const active=(href:string)=>href==="/admin"?pathname==="/admin":pathname===href||pathname.startsWith(`${href}/`);const style=(href:string)=>`inline-flex items-center gap-2 px-3 py-2 text-sm font-medium transition ${active(href)?"text-black underline decoration-2 underline-offset-8":"text-zinc-700 hover:text-black"}`;return <header className="sticky top-0 z-40 border-b bg-white/95 backdrop-blur"><nav className="mx-auto flex h-16 max-w-6xl items-center gap-2 px-4 sm:px-6"><Link href="/admin" className="mr-auto inline-flex items-center gap-2 text-lg font-bold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-zinc-950 text-white"><ShoppingBag size={17}/></span><span className="hidden xs:inline">Babs Shop</span></Link><div className="hidden items-center gap-1 md:flex">{links.map(({href,label,icon:Icon})=><Link key={href} href={href} className={style(href)}><Icon size={17}/>{label}</Link>)}<form action={logout}><button className="inline-flex items-center gap-2 px-3 py-2 text-sm font-medium text-zinc-700 hover:text-black"><LogOut size={17}/>Déconnexion</button></form></div><button onClick={()=>setOpen(!open)} className="rounded-lg p-2 hover:bg-zinc-100 md:hidden" aria-label="Menu administration">{open?<X size={21}/>:<Menu size={21}/>}</button></nav>{open&&<div className="border-t bg-white px-4 py-3 md:hidden"><div className="mx-auto grid max-w-6xl gap-1">{links.map(({href,label,icon:Icon})=><Link onClick={()=>setOpen(false)} key={href} href={href} className={`${style(href)} ${active(href)?"bg-zinc-100":""}`}><Icon size={17}/>{label}</Link>)}<div className="my-2 border-t"/><form action={logout}><button className="inline-flex w-full items-center gap-2 px-3 py-2 text-sm font-medium text-zinc-700"><LogOut size={17}/>Déconnexion</button></form></div></div>}</header>}
